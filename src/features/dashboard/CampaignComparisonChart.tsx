import{CartesianGrid,Legend,Line,LineChart,ResponsiveContainer,Tooltip,XAxis,YAxis}from"recharts";import type{DecisionRow}from"@/services/decision.service";
const money=new Intl.NumberFormat("es-AR",{style:"currency",currency:"ARS",notation:"compact",maximumFractionDigits:1});
export function CampaignComparisonChart({previous,current}:{previous:DecisionRow[];current:DecisionRow[]}){const data=previous.map(old=>{const now=current.find(x=>x.farmId===old.farmId);return{name:old.farmName,anterior:old.marginPerHa,actual:now?.marginPerHa??null}});return<div className="campaign-comparison-chart"><header><strong>Evolución del margen por hectárea</strong><span>Comparación 2025/26 frente a 2026/27</span></header><ResponsiveContainer width="100%" height={260}><LineChart data={data} margin={{top:10,right:24,left:5,bottom:8}}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="name" tick={{fontSize:10}}/><YAxis tick={{fontSize:9}} tickFormatter={x=>money.format(x)}/><Tooltip formatter={(value)=>money.format(Number(value))}/><Legend wrapperStyle={{fontSize:10}}/><Line type="monotone" dataKey="anterior" name="2025/26" stroke="#8493a4" strokeWidth={2} dot={{r:4}}/><Line type="monotone" dataKey="actual" name="2026/27" stroke="var(--farm-color)" strokeWidth={3} dot={{r:4}}/></LineChart></ResponsiveContainer></div>}
